import { css } from '@emotion/core'
import 'utils/typography'

export default {
  background: css({
    minHeight: '100vh',
    display: 'flex',
    backgroundColor: '#fff',
    paddingTop: 100,
    paddingBottom: 100,
  }),
}
